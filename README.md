
  # dApp studio

  This is a code bundle for dApp studio. The original project is available at https://www.figma.com/design/vv079InfLPhQSqT2UUIax2/dApp-studio.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # dapp-studio-frontend
